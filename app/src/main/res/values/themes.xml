<resources>
    <!-- Base app theme — uses AppCompat which is already included via camera-view -->
    <!-- Note: Material3 theming is handled by Jetpack Compose MaterialTheme, not XML -->
    <style name="Theme.VisuraCam" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>

    <!-- Fullscreen theme for camera viewfinder -->
    <style name="Theme.VisuraCam.FullScreen" parent="Theme.VisuraCam">
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
        <item name="android:windowTranslucentStatus">true</item>
        <item name="android:windowTranslucentNavigation">true</item>
    </style>
</resources>
